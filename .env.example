############################################################
# RealLife Backend Environment Example
# ------------------------------------
# 이 파일은 예시 파일입니다.
# 실제 실행 시에는 ".env"로 복사해서 사용하세요.
#
#   cp .env.example .env
#
# ✅ .env는 깃에 올리지 마세요.
# ✅ .env.example은 깃에 올려서 다른 사람들이 참고하게 합니다.
############################################################

############################
# MySQL
############################
MYSQL_ROOT_PASSWORD=CHANGE_ME_ROOT
MYSQL_DATABASE=backend
MYSQL_USER=backend
MYSQL_PASSWORD=backendpw
MYSQL_PORT=3307

############################
# Spring Profile
############################
# dev   : 로컬/개발 기본
# prod  : 운영(HTTPS + 도메인)
SPRING_PROFILES_ACTIVE=dev

############################
# Redis
############################
SPRING_DATA_REDIS_HOST=redis
SPRING_DATA_REDIS_PORT=6379

############################
# JWT
############################
# 최소 64자 이상 랜덤 문자열 권장
JWT_SECRET=CHANGE_ME_TO_LONG_RANDOM_SECRET_KEY_64_CHARS_MINIMUM
JWT_ACCESS_TOKEN_EXP_MINUTES=60
JWT_REFRESH_TOKEN_EXP_DAYS=14

############################
# File Upload
############################
FILE_UPLOAD_DIR=/app/uploads

############################
# Public Base URL
############################
PUBLIC_BASE_URL=http://localhost

############################
# CORS
############################
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000

############################
# Cookie (인증 매우 중요)
############################
# ===== 개발(http) =====
APP_COOKIE_SAME_SITE=Lax
APP_COOKIE_SECURE_MODE=false

# ===== 운영(HTTPS + 도메인) =====
# APP_COOKIE_SAME_SITE=None
# APP_COOKIE_SECURE_MODE=true
# APP_COOKIE_DOMAIN=.example.com

############################
# App Meta (선택)
############################
# 없으면 compose에서 기본값(dev/unknown)으로 들어가도록 되어 있음
# APP_VERSION=dev
# APP_BUILD_TIME=unknown

############################
# GHCR (docker-compose.prod.yml에서 필수)
############################
# 예: programer5/reallife-backend
# docker-compose.prod.yml에서 이미지:
#   ghcr.io/${GHCR_REPO}/reallife-app:latest
#   ghcr.io/${GHCR_REPO}/reallife-nginx:latest
GHCR_REPO=YOUR_GITHUB_OWNER/YOUR_REPO